# Wolf Pack - Claude Code 规则固化与工作流插件

> 🐺 五技能体系：头脑风暴 → 看板 → 执行 → 洞察 → 记忆

## 功能特性

- **目标澄清** - 通过交互式提问确定目标和验收标准
- **任务拆解** - 将任务拆解为可并行执行的单元
- **工作流执行** - 预定义工作流模板，支持多 Agent 并发
- **洞察分析** - 自动分析会话并提取问题、模式和亮点
- **规则持久化** - 通过 Hook 自动保存和加载项目规则

## 五技能概览

| 技能 | 命令 | 功能 |
|------|------|------|
| 头脑风暴 | `/wolf-brainstorm` | 澄清目标，确定验收标准 |
| 看板 | `/wolf-board` | 拆解为可并行执行的单元 |
| 执行 | `/wolf-pack` | 执行工作流，多 agent 并发 |
| 洞察 | `/wolf-insights` | 分析问题、模式和亮点 |
| 记忆 | `/wolf-remember` | 记录问题、决策、模式 |

## 典型工作流

```
1. /wolf-brainstorm     # 澄清目标
2. /wolf-board          # 选择任务，拆解单元
3. /wolf-pack           # 执行（多 agent 并发）
4. /wolf-insights        # 分析亮点
5. /wolf-remember        # 记录模式
```

## 安装

```bash
# 克隆到本地插件目录
git clone https://github.com/wolf-pack/claude-code-plugin.git ~/.claude/plugins/wolf-pack

# 或复制到项目插件目录
cp -r wolf-pack /path/to/project/.wolf/plugins/
```

## 目录结构

```
wolf-pack/
├── .wolf-plugin/          # 插件元数据
├── hooks/                   # Hook 脚本
├── commands/                # 命令定义
├── skills/                  # 技能定义
├── agents/                  # Subagent 模板
├── workflows/               # 预定义工作流
├── templates/               # 模板文件
└── README.md
```

## 命令详解

### /wolf-brainstorm - 头脑风暴

通过交互式提问澄清目标和验收标准。

```bash
/wolf-brainstorm              # 开始头脑风暴
```

**提问框架**：
1. 目标描述 - "你想要完成什么？"
2. 背景上下文 - "为什么需要这个？当前情况是什么？"
3. 约束条件 - "有什么限制或约束？"
4. 验收标准 - "如何知道目标达成了？"

**输出**：计划摘要（目标/验收/约束/预估）

### /wolf-board - 任务看板

汇总多来源任务，按优先级展示，支持拆解为执行单元。

```bash
/wolf-board                           # 显示所有任务
/wolf-board --source=tasks            # 只显示 Claude 任务
/wolf-board --source=issues           # 只显示问题
/wolf-board --priority=high          # 只显示高优先级
/wolf-board --breakdown               # 拆解任务为执行单元
```

**任务来源**：
- `tasks` - Claude 内置任务系统
- `projects` - TODO.md, TASKS.md
- `issues` - rules/issues.md
- `session` - 当前会话

---

### /wolf-pack - 执行与调度

执行工作流或并行执行单元。

```bash
# 工作流模式
/wolf-pack multi-debug           # 多问题并行调试
/wolf-pack feature-dev           # 功能开发流程
/wolf-pack refactor              # 重构工作流

# 工作流管理（原 wolf-flow）
/wolf-pack create                # 创建新工作流
/wolf-pack list                  # 列出所有工作流
/wolf-pack validate [workflow]    # 验证工作流

# 并行执行单元模式
/wolf-pack unit1 unit2 unit3     # 并发执行多个单元
```

**预定义工作流**：

| 工作流 | 用途 | 并发 |
|--------|------|------|
| multi-debug | 多问题并行调试 | 是 |
| feature-dev | 完整功能开发 | 否 |
| refactor | 代码重构 | 部分 |
| task-board | 任务看板执行 | 是 |

---

### /wolf-insights - 洞察分析

分析当前会话并提取洞察存储到项目记忆。

```bash
/wolf-insights               # 分析所有类别
/wolf-insights --category=issues
/wolf-insights --output=summary
```

**分析类别**：
- `issues` - 问题识别 → `rules/issues.md`
- `patterns` - 模式发现 → `rules/patterns.md`
- `highlights` - 亮点捕捉 → `rules/patterns.md`

---

### /wolf-remember - 规则记录

手动记录到项目规则文件。

```bash
/wolf-remember init          # 初始化规则结构
/wolf-remember issue "问题描述"
/wolf-remember decision "决策描述"
/wolf-remember pattern "模式描述"
```

**存储位置**：
- `issue` → `rules/issues.md`
- `decision` → `rules/decisions.md`
- `pattern` → `rules/patterns.md`
- `context` → `rules/context.md`

## 项目规则结构

```
project-root/
├── .wolf.md             # 项目级指令入口
└── rules/                 # 项目规则目录
    ├── context.md         # 项目上下文
    ├── decisions.md       # 决策记录
    ├── patterns.md        # 代码模式 + 亮点
    └── issues.md          # 问题记录
```

## 工作流管理

### 创建工作流

```bash
/wolf-pack create
```

交互式创建工作流，支持：
- 前置条件检查（加权评分）
- 破坏性操作确认
- 步骤依赖关系
- 并发/顺序执行
- 偏差处理策略

### 工作流格式

```markdown
## [工作流名称]

### 描述
[工作流的简短描述]

### 触发条件
[什么情况下使用此工作流]

### 前置条件 (preconditions)

```yaml
checks:
  - name: dependencies_installed
    description: 检查所需依赖是否已安装
    weight: 10
  # ...

threshold: 60

destructive:
  - delete_files: true
```

### 执行配置
- **模式**: strict | lenient
- **偏差处理**: track | block | warn

### 步骤
#### Step 1: [步骤名称]
- **关键**: true/false
- **依赖**: []
- **Agent**: general-purpose
- **并行**: false
- **输出**: [预期结果]
```

## Hook 机制

### SessionStart Hook

- 检查 `.wolf.md` 和 `rules/` 目录
- 读取并注入项目规则到系统上下文
- 如不存在，提供初始化提示

### SessionEnd Hook

- 记录文件变化到 `.wolf/trace/`
- 追加会话元数据到规则文件
- 记录会话统计信息

**跟踪目录**：`.wolf/trace/`
- `YYYY-MM-DD-files.md` - 文件变化记录
- `YYYY-MM-DD-stats.md` - 会话统计

## 配置

### 执行模式

| 模式 | 说明 |
|------|------|
| strict | 所有步骤必须执行 |
| lenient | 关键步骤必须完成 |

### 偏差处理

| 模式 | 说明 |
|------|------|
| track | 记录偏差并继续 |
| block | 偏差时停止执行 |
| warn | 偏差时警告但继续 |

## 执行单元拆解

`/wolf-board --breakdown` 可将任务拆解为可并行执行的单元：

```yaml
执行单元格式:
  id: 唯一标识
  description: 简要描述
  files: 涉及文件
  dependencies: 依赖的 unit id
  estimated_time: 预估时间
```

**拆解原则**：
- 按文件边界拆分（避免冲突）
- 按功能模块拆分（高内聚）
- 按依赖关系排序

## 多 Agent 并发

### 文件冲突处理

```yaml
工作流声明:
  file_lock: ["path/to/file"]

执行时:
  - 检查文件是否被锁定
  - 获取锁的 agent 先执行
  - 其他 agent 等待或跳过
```

### 依赖关系处理

```yaml
工作流声明:
  depends_on: [step_id_1, step_id_2]

执行时:
  - 分析依赖图
  - 拓扑排序
  - 按顺序执行
```

### 上下文共享

```yaml
task-runner 参数:
  - project_context: 项目上下文
  - task_goal: 任务目标
  - task_dependencies: 依赖信息
```

## 开发

### 添加新技能

1. 在 `skills/` 创建新目录
2. 创建 `SKILL.md` 文件
3. 在对应的 command 中引用

### 添加新工作流

1. 使用 `/wolf-pack create` 交互式创建
2. 或直接在 `workflows/` 创建 `.md` 文件
3. 使用 `/wolf-pack validate` 验证

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License
