# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

Wolf Pack 是一个 Claude Code 插件，提供多层级记忆系统、头脑风暴、任务看板与并行执行能力。

**技术栈**: JavaScript/Node.js (Hook 脚本), Markdown (配置/文档), JSON (元数据)

**版本**: 定义在 `.claude-plugin/plugin.json`

## 核心命令

| 命令 | 功能 |
|------|------|
| `/wolf-brainstorm` | 交互式目标澄清，生成计划摘要 |
| `/wolf-board` | 任务聚合与拆解，生成执行单元 |
| `/wolf-pack` | 工作流执行，Agent Team 并发调度 |
| `/wolf-insights` | 分析会话，提取问题/模式/亮点 |
| `/wolf-memory` | 进化记忆系统：记录/加载/搜索/热点追踪/经验提取 |
| `/wolf-session` | 会话状态管理 (跨平台支持) |
| `/wolf-terminal` | Windows Terminal 分屏启动器 |

## 架构概览

```
wolf-pack/
├── .claude-plugin/       # 插件元数据 (plugin.json, marketplace.json)
├── hooks/                # Hook 脚本 (Node.js)
│   ├── hooks.json        # Hook 配置 (shell: node)
│   ├── session-start.js  # 会话开始：加载项目规则
│   └── session-end.js    # 会话结束：记录会话洞察
├── commands/             # 命令定义 (frontmatter: argument-hint, description)
├── skills/               # 技能实现 (SKILL.md 文件)
├── workflows/            # 工作流模板 (前置条件、步骤、Agent 配置)
├── templates/            # 文件模板
└── output-styles/        # 输出样式定义
```

### 记忆系统层级

```
L0: ~/.claude/CLAUDE.md                 # 全局指令
L1: .wolf.md                            # 项目入口索引
L2: .claude/rules/                      # 二级记忆
    ├── index.md                        # 主索引
    ├── decisions/                      # 技术决策
    ├── patterns/                       # 成功模式
    │   ├── component/                  # 组件模式
    │   ├── api/                        # API 模式
    │   ├── workflow/                   # 工作流模式
    │   ├── architecture/               # 架构模式
    │   └── experiences/                # 经验模式（新增）
    ├── issues/                         # 问题记录
    │   ├── open/                       # 未解决问题
    │   ├── solved/                     # 已解决问题
    │   ├── hotspots/                  # 高发点（新增）
    │   ├── stats.json                 # 统计数据（新增）
    │   └── config.yaml                # 配置文件（新增）
    ├── context/                        # 项目上下文
    └── archived/                       # 归档记忆
L3: ~/.claude/rules/                    # 用户级记忆（新增）
```

## 工作流系统

预定义工作流位于 `workflows/`：

| 工作流 | 用途 | 并发 |
|--------|------|------|
| `feature-dev` | 完整功能开发 | 否 |
| `multi-debug` | 多问题并行调试 | 是 |
| `refactor` | 代码重构 | 部分 |
| `task-board` | 任务看板执行 | 是 |

### 工作流执行流程

```
1. 加载工作流定义
2. 前置条件检查 (加权评分，需达到 threshold)
3. 破坏性操作确认 (如有)
4. Agent Team 创建与任务分配
5. 并发/顺序执行步骤
6. 结果整合与报告
```

### Agent Team 工具链

```
TeamCreate() → TaskCreate() → TaskUpdate(owner/blockedBy)
→ SendMessage(broadcast) → TaskList() → shutdown_request → TeamDelete()
```

- **文件冲突处理**: 检查各步骤涉及文件，重叠则设置 `addBlockedBy` 依赖
- **并发控制**: 无冲突任务 `owner=null`，有冲突任务设置依赖

## Hook 机制

配置文件 `hooks/hooks.json` 定义了 SessionStart 和 SessionEnd hook。

**SessionStart**: 读取 `.wolf.md` 和 `.claude/rules/` 目录，注入项目规则到系统上下文，检查高发点警告

**SessionEnd**: 记录文件变化到 `.wolf/trace/`，追加会话元数据，自动提升优质记忆到用户级

## 开发指南

### 添加新命令

1. 在 `commands/` 创建 `.md` 文件
2. Frontmatter 定义: `argument-hint` 和 `description`
3. 在对应 `skills/` 目录创建 `SKILL.md`

### 添加新工作流

```bash
/wolf-pack create              # 交互式创建
/wolf-pack list                # 列出所有工作流
/wolf-pack validate [workflow] # 验证
```

## 项目约定

1. **多文件功能开发前先展示架构** - 涉及 3+ 文件必须先展示：文件结构树、接口/类设计、模块依赖
2. **函数式思想** - 一个函数实现一个功能，避免副作用
3. **Markdown 作为配置** - 命令、技能、工作流均使用 Markdown 定义
4. **记忆持久化** - 所有重要决策、模式、问题都应记录到 `.claude/rules/`

## 输出样式

项目使用自定义输出样式 `wolf-pack:Wolf Pack`：

- **强制前缀**: 所有输出以 `🐺: 嗷~~` 开头
- **视觉分隔**: `═══` 主要分区，`─────────` 子分区
- **信息类型**: 📋 计划、✅ 成功、⚠️ 警告、❌ 错误、💡 提示

## 权限配置

`.claude/settings.local.json` 定义了必要的 Bash 权限：Git 操作、PowerShell 脚本、Hook 脚本执行

## 关键文件

| 文件 | 用途 |
|------|------|
| `.wolf.md` | 项目级指令入口 |
| `.claude/rules/index.md` | 记忆索引汇总（含热点统计） |
| `skills/wolf-pack/SKILL.md` | 工作流执行核心逻辑 |
| `.claude/rules/issues/config.yaml` | 进化记忆系统配置 |
| `.claude/rules/issues/stats.json` | 问题统计数据 |
| `scripts/promote-memory.js` | 记忆提升脚本 |
| `docs/evolved-memory-system.md` | 进化记忆系统文档 |
| `output-styles/wolf-pack.md` | 完整输出样式规范 |
